# 📊 Statistics Module

Модуль для отображения административной статистики в социальной сети на Flask.

## Возможности

- Общее количество пользователей
- Количество постов
- Активные авторы (с хотя бы одним постом)
- Забаненные пользователи
- Посты за последние 7 дней

## Установка

1. Помести папку `statistics/` в корень проекта.
2. Зарегистрируй blueprint в `app.py`:

```python
from statistics import statistics_bp
app.register_blueprint(statistics_bp, url_prefix='/admin')
